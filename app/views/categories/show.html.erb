<h1><%= @category.category_name %></h1>

<% @category.articles.each do |article| %>
  <a href="/articles/<%= article.id %>"><div><%= article.title %>:</div></a>

  <div><%= article.description %></div>
  <br>
<% end %>

<a href="/">HOMEPAGE</a>

<!-- <a href="/categories/<%= @category.id %>/edit">edit your page</a>
 -->
<span><%= link_to "edit", edit_category_path(@category) %></span>

<span><%= link_to "delete", category_path(@category), :method => :delete %></span>
